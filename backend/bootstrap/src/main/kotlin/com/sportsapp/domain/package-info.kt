/**
 * Domain Layer
 *
 * 책임: 순수 비즈니스 로직 (Rich Domain Model).
 *       Repository / Gateway / DomainEventPublisher interface 정의.
 *
 * 의존: 없음 (순수 Kotlin/Java 표준 라이브러리만).
 * 금지: presentation / application / infrastructure import.
 */
package com.sportsapp.domain
