server:
  port: 8080

spring:
  application:
    name: sports-application
  config:
    use-legacy-processing: false

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
