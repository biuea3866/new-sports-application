# E2E-R2 사업자 포털 상품 폼 검증 메시지 (zod 4.x 마이그레이션 영향)

## 메타
- severity: Major
- layer-hint: FE
- related-files:
  - web/app/portal/products/product-form-schema.ts
- related-ticket: none
- estimated-duration: 2m

## 사전 조건
- DB 시드: `qa/e2e/fixtures/operator-multi-role.sql`
  - operator-C (GOODS_SELLER) + 본인 소유 상품 1건 (이름 "테스트 상품", ACTIVE, 수량 10)
- 인증 상태: operator-C로 로그인
- 환경 변수: 없음

## 시나리오 (Given/When/Then 한 줄)

### 신규 등록 폼 (`/portal/products/new`)
| ID | Given | When | Then |
|---|---|---|---|
| E2E-R2-01 | operator-C 로그인 + 신규 등록 폼 진입 | 가격 입력값을 비운 채 저장 버튼 클릭 시 | 가격 필드 아래에 zod 4.x 마이그레이션 전과 동일한 "가격을 입력해주세요" 검증 메시지가 표시된다 |
| E2E-R2-02 | operator-C 로그인 + 신규 등록 폼 진입 | 카테고리 선택을 비운 채 저장 버튼 클릭 시 | 카테고리 필드 아래에 "카테고리를 선택해주세요" 메시지가 표시된다 |
| E2E-R2-03 | operator-C 로그인 + 신규 등록 폼 진입 | 이미지 URL 필드에 잘못된 형식("not-a-url")을 입력 후 저장 시 | 이미지 URL 필드 아래에 "올바른 URL을 입력해주세요" 메시지가 표시된다 |
| E2E-R2-04 | operator-C 로그인 + 신규 등록 폼 진입 | 가격에 숫자가 아닌 문자열("abc")을 입력 후 저장 시 | 가격 필드 아래에 "숫자를 입력해주세요" 또는 동등한 type 검증 메시지가 표시된다 (zod 4.x message 속성으로 노출) |
| E2E-R2-05 | operator-C 로그인 + 신규 등록 폼 진입 | 모든 필드를 유효하게 입력 후 저장 시 | 폼이 정상 제출되어 상품 상세 페이지로 이동하고 검증 메시지는 표시되지 않는다 |

### 수정 폼 (`/portal/products/[id]`)
| ID | Given | When | Then |
|---|---|---|---|
| E2E-R2-06 | operator-C 로그인 + 본인 상품 수정 폼 진입 | 가격을 비운 채 저장 시 | "가격을 입력해주세요" 메시지가 신규 등록 폼과 동일하게 표시된다 |
| E2E-R2-07 | operator-C 로그인 + 본인 상품 수정 폼 진입 | 카테고리를 빈 상태로 변경 후 저장 시 | "카테고리를 선택해주세요" 메시지가 표시된다 |

### 재고 복원 폼
| ID | Given | When | Then |
|---|---|---|---|
| E2E-R2-08 | operator-C 로그인 + 재고 복원 폼 진입 | 복원 수량을 비운 채 제출 시 | 수량 필드 아래에 "수량을 입력해주세요" 또는 동등한 메시지가 표시된다 |
| E2E-R2-09 | operator-C 로그인 + 재고 복원 폼 진입 | 복원 수량에 음수(-1)를 입력 후 제출 시 | "1 이상의 수량을 입력해주세요" 또는 동등한 범위 검증 메시지가 표시된다 |

## 회귀 케이스 (변경 후에도 깨지면 안 되는 기존 동작)
| ID | 케이스 |
|---|---|
| E2E-R2-R01 | zod 4.x 마이그레이션 전후로 모든 검증 메시지 문자열이 1자도 변경되지 않고 동일하게 노출된다 |
| E2E-R2-R02 | 검증 메시지는 한국어로 표시되고 i18n fallback 영문 메시지가 노출되지 않는다 |
| E2E-R2-R03 | 폼 제출 실패 시 첫 번째 검증 실패 필드로 자동 포커스가 이동한다 |

## 엣지 케이스
| ID | 케이스 |
|---|---|
| E2E-R2-E01 | 모든 필드가 비어있는 상태로 저장 시 모든 필드의 검증 메시지가 동시에 표시된다 |
| E2E-R2-E02 | 가격 필드에 매우 큰 값(2^53 초과)을 입력 시 type 검증 또는 범위 검증 메시지가 표시되고 폼 제출이 차단된다 |
| E2E-R2-E03 | 검증 실패 상태에서 필드를 유효한 값으로 수정하면 해당 필드의 검증 메시지가 즉시 사라진다 |
| E2E-R2-E04 | 비로그인 상태로 `/portal/products/new` 진입 시 로그인 페이지로 리다이렉트된다 |
| E2E-R2-E05 | 다른 operator 소유 상품의 수정 폼(`/portal/products/[id]`) 직접 접근 시 403 또는 권한 오류 페이지가 표시된다 |
